package other

import basic.VisibilityModifier

class VisibilityModifierConnect {

    fun main() {
        var connect = VisibilityModifier()
        connect.publicMultiplyTwoNumbers(1, 2)
        connect.internalMultiplyTwoNumbers(3, 4)
    }
}